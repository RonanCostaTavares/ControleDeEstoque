import {StyleSheet, Platform} from 'react-native'


export default css = StyleSheet.create({

    LogoLogin: {
        marginTop:10,
        marginBottom:10,
        alignItems: "center",
        
    },

});